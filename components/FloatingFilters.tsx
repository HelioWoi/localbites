
import React from 'react';

// FloatingFilters component removed - all filters now in single modal via Filter icon
// Keeping file for backwards compatibility but component is no longer used

const FloatingFilters: React.FC = () => {
  return null;
};

export default FloatingFilters;
